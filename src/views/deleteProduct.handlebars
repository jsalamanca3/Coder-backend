<h1>Administrador - Eliminar Productos</h1>
  <ul>
    {{#each products}}
      <li>{{title}} - {{description}} <button onclick="deleteProduct('{{_id}}')">Eliminar</button></li>
    {{/each}}
  </ul>
<script src="/js/deleteProduct.js"></script>